[project]
name = "compass"
version = "0.1.0"
description = "Spanish public tender radar"
readme = "README.md"
authors = [
    { name = "Alan", email = "alanfdiosdado@gmail.com" }
]
requires-python = ">=3.13"
dependencies = []

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
